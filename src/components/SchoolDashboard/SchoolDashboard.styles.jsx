import styled  from 'styled-components';

export const SchoolDashboardContainer = styled.div`
    padding: 20px;
    flex: 1;
    flex-direction: column;

    .btn-action-wrp{
        display: flex;
        flex-direction: row;
        gap: 15px;
        .btn-add-school{
            margin-bottom: 20px;
        }
    }
`;